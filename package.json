{
  "name": "all-eslint-rule-set",
  "version": "0.1.0",
  "description": "A set of the all available ESLint rules",
  "repository": "shinnn/all-eslint-rule-set",
  "scripts": {
    "pretest": "eslint --config @shinnn/node-legacy index.js test.js",
    "test": "node --throw-deprecation --track-heap-objects node_modules/.bin/ava"
  },
  "license": "MIT",
  "files": [
    "index.js"
  ],
  "keywords": [
    "eslint",
    "setting",
    "config",
    "configuration",
    "set",
    "list",
    "rule",
    "rules"
  ],
  "dependencies": {
    "all-eslint-rules": "^1.0.1"
  },
  "devDependencies": {
    "@shinnn/eslint-config-node-legacy": "^1.0.1",
    "array-difference": "^0.0.1",
    "ava": "^0.10.0"
  }
}
